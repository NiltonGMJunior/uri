#include <bits/stdc++.h>

int main()
{
    // While a zero is not read
    while (true)
    {
        // Number of coaches
        unsigned short int n;
        std::cin >> n;

        // Break condition
        if (n == 0) break;

        // Loops for the n coaches train
        while (true)
        {
            
        }
    }   
    return 0;
}